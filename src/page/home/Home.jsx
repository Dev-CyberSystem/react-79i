const Home = () => {
  return {
    /* 
      Landing page
      -Navbar
      -carrousel
      -cards
      -section (Informacion)
      -footer */
  };

  //Empezando la tarea de react
};

export default Home;
